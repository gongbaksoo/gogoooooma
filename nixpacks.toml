providers = ["python"]

[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["cd api && pip install -r requirements.txt"]

[start]
cmd = "cd api && uvicorn index:app --host 0.0.0.0 --port $PORT"
